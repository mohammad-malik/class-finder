{
  "builds": [
    {
      "src": "api/app.py",
      "use": "@vercel/python",
      "config": {
        "includeFiles": ["requirements.txt", "api/bin/**", "api/data/**"]
      }
    }
  ],
  "routes": [
    { "src": "/(.*)", "dest": "api/app.py" }
  ]
}